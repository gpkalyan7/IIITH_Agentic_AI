# Secrets. .env.example is committed; .env never is.
.env

__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.DS_Store

# Generated by a run, not source. These are reproducible with
# `python demo.py --all` and are shipped in the submission ZIP rather than
# committed. They also quote message bodies verbatim, including the synthetic
# broker credential in m003, which outbound DLP scanners flag.
trace.jsonl
_probe_trace.jsonl
decisions.json
dashboard.html
dashboard.json
data/prefs.json
data/state.json
data/llm_cache/
outbox/*
!outbox/.gitkeep
